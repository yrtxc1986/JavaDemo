server:
  port: 0  servlet:    session:      timeout: 1800
spring:  session:    store-type: redis
    redis:      flush-mode: on-save
      namespace: spring:session  redis:    host: localhost
    port: 6379